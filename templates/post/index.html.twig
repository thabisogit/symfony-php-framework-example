{% extends 'main/custom.html.twig' %}

{% block title %}{{ welcom }}{% endblock %}

{% block body %}

 		{% if posts %}
<table id="posts" class="table table-striped">
	<thead>
	<tr>
		<th>Name</th>
		<th>Action</th>
	</tr>
</thead>
<tbody>
	{% for post in posts %}
	<tr>
		<td>{{post.title }}</td>
		<td><a href="show/{{ post.id }}" class="btn btn-dark">Show</a>
		
		<a href="/post/edit/{{ post.id }}" class="btn btn-light">Edit</a>
		<a href="#" class="btn btn-danger delete-contact" data-id="{{ post.id }}">Delete</a>
		<a href="delete/{{ post.id }}" class="btn btn-danger delete-contact" data-id="{{ post.id }}">Delete2</a></td>
		
	</tr>
	{% endfor %}
</tbody>
</table>
{% else %}
	<p>No Articles Found</p>
{% endif %}
 	{% endblock %}

